{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "SmartComponents": {
    "ApiKey": "",
    // This is the name from Azure OpenAI studio, not from the Azure Portal OpenAI resource (unless you're using an OpenAI key)
    "DeploymentName": "",

    // Required for Azure OpenAI only. If you're using OpenAI, remove the following line.
    "Endpoint": ""
  },
  "AllowedHosts": "*"
}
